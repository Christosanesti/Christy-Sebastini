# Project thumbnails / logos

Place project logos or thumbnails here for the projects list and detail pages.

**Example:** `logo-ubisoft.png` (raster export from Logo-Ubisoft.pdf) for Ubisoft project thumbnail.

Paths are optional in `lib/projects.ts`; if missing, the UI shows a fallback (no image). See PRD Assets inventory.
